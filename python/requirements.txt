# Microsoft Agent Framework for Python + the A2A SDK.
# Checked against PyPI on 2026-08-23:
#   agent-framework-core    1.15.0        (stable)
#   agent-framework-a2a     1.0.0b260821  (beta -- the A2A integration is still pre-1.0)
#   agent-framework-openai  1.14.0        (stable; also covers Azure OpenAI endpoints)
#   a2a-sdk                 1.1.2

# Client: wraps a remote A2A agent as an Agent Framework agent.
agent-framework-a2a==1.0.0b260821

# Host: the A2A server routes need Starlette + sse-starlette, pulled in by the
# http-server extra. uvicorn actually serves it.
a2a-sdk[http-server]==1.1.2
uvicorn>=0.34

# Reads python/.env (copy the repo root's .env.template). Pulled in transitively
# today, pinned here because both the host and the client import it directly.
python-dotenv>=1.0

# Only needed to put a real model behind the agent. Leave it installed; the host
# falls back to a scripted agent when AzureOpenAI is unconfigured.
agent-framework-openai==1.14.0
